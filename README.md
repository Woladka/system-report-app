# system-report-app

Uses python scripts and libraries like psutil and shutil to calculate disk space and CPU usage to deliver system health.
Creating a website (still in work ) to generate a PDF of system health which can be downloade dby user.

